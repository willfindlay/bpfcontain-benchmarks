name: allow

# Spawn untainted and privileged so we allow everything
defaultTaint: false
privileged: true
# Complain just in case something slips through the cracks
complain: true

allow:
  - capability: any
  - net: any
